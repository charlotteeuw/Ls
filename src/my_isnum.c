/*
** EPITECH PROJECT, 2023
** my_str_isalpha
** File description:
** return 1 if the string given as parameter
** only contain alphabetical char
*/

int is_num(char c)
{
    if (c > 47 && c < 58)
        return (1);
    return (0);
}
